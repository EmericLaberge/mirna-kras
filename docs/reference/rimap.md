# Interaction computation — Stage 1

miRNA–transcript affinity prediction via the RIMap-RISC web API or via MiRScanS (local).

## `scripts.rimap_pipeline`

Web API variant. Recommended by default — no Java install required.

::: scripts.rimap_pipeline

## `scripts.rimap_pipeline_local`

Local variant with Java MiRScanS + MC-Flashfold. Faster but requires a full install.

::: scripts.rimap_pipeline_local